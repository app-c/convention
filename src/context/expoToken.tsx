/* eslint-disable react-hooks/exhaustive-deps */
import React from "react";
import * as Notifications from "expo-notifications";
import { Alert, Platform } from "react-native";

export function ExpoToken() {
   const token = null;

   return { token };
}
